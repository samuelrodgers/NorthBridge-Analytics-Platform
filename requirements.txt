# Northbridge Analytics Platform — Python dependencies
# Covers both bouncer.py (FastAPI) and scripts/NAP_ingestion/ (pipeline).
# Install with: pip install -r requirements.txt

# API (bouncer.py)
fastapi>=0.115.0
uvicorn>=0.30.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
pydantic[email]>=2.0.0

# Pipeline (ingestion / transform / ML)
numpy>=1.26.0
pandas>=2.0.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
requests>=2.28.0
scikit-learn>=1.3.0
